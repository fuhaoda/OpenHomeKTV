# Media paths list (non-recursive). One path per line.
# Lines starting with # are ignored. key=value format is also accepted.
# Examples:
mv_path_0=/path/to/your/mv-folder
mv_path_1=/path/to/another/mv-folder
# /path/to/extra-folder
